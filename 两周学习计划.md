# KVstorageBaseRaft-cpp 两周学习计划

## 使用方式

这份计划不是泛读清单，而是按这个仓库的真实代码路径安排的精读顺序。

学习时遵循三个原则：

1. 先打通一条完整请求链路，再看局部实现。
2. 每天必须输出自己的图和文字总结，不能只看代码。
3. 每读完一个函数，都要回答“它解决了什么问题，依赖什么状态，可能在哪些异常场景出错”。

---

## 项目主线

真正需要优先理解的是这条链路：

`Clerk -> KvServer RPC -> Raft::Start -> 日志复制/提交 -> applyChan -> KvServer 状态机 -> RPC 返回客户端`

围绕这条主线，核心文件如下：

- 服务端入口：[example/raftCoreExample/raftKvDB.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/example/raftCoreExample/raftKvDB.cpp)
- 客户端入口：[example/raftCoreExample/caller.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/example/raftCoreExample/caller.cpp)
- 客户端逻辑：[src/raftClerk/clerk.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/clerk.cpp)
- KV 服务层：[src/raftCore/kvServer.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp)
- Raft 核心：[src/raftCore/raft.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp)
- 持久化：[src/raftCore/Persister.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/Persister.cpp)
- RPC 通道：[src/rpc/mprpcchannel.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/rpc/mprpcchannel.cpp)

---

## 第 1 天

### 目标

只做一件事：跑通并讲清一次请求从客户端发出到返回结果的全过程。

### 阅读顺序

1. [example/raftCoreExample/caller.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/example/raftCoreExample/caller.cpp)
2. [src/raftClerk/include/clerk.h](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/include/clerk.h)
3. [src/raftClerk/clerk.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/clerk.cpp)
4. [src/raftCore/include/kvServer.h](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/include/kvServer.h)
5. [src/raftCore/kvServer.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp)

### 精读函数

- [Clerk::Init](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/clerk.cpp#L78)
- [Clerk::Get](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/clerk.cpp#L12)
- [Clerk::PutAppend](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftClerk/clerk.cpp#L41)
- [KvServer::Get](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L79)
- [KvServer::PutAppend](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L212)

### 你要回答的问题

1. `Clerk` 为什么要维护 `m_recentLeaderId`？
2. 为什么客户端在 `ErrWrongLeader` 时不修改 `requestId`？
3. `KvServer` 为什么不直接执行 `Put/Get`，而是先把请求交给 `Raft::Start`？
4. `waitApplyCh` 是什么，它解决了什么同步问题？
5. 为什么 `Get` 和 `PutAppend` 的超时处理逻辑不完全一样？

### 当天产出

你必须手写一张时序图，包含以下对象：

- `caller`
- `Clerk`
- `KvServer`
- `Raft leader`
- `Raft follower`
- `applyChan`
- `waitApplyCh`

### 当天验收标准

如果别人问你“为什么客户端请求不能直接在 leader 本地改内存后返回”，你能基于一致性回答清楚。

---

## 第 2 天

### 目标

吃透 `KvServer` 在整个系统中的职责边界。你要明白它不是数据库本体，而是“Raft 与状态机之间的协调层”。

### 阅读顺序

1. 重读 [src/raftCore/include/kvServer.h](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/include/kvServer.h)
2. 精读 [src/raftCore/kvServer.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp)
3. 快速扫一遍 [src/skipList/include/skipList.h](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/skipList/include/skipList.h)

### 精读函数

- [KvServer::GetCommandFromRaft](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L166)
- [KvServer::ReadRaftApplyCommandLoop](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L281)
- [KvServer::SendMessageToWaitChan](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L325)
- [KvServer::IfNeedToSendSnapShotCommand](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L342)
- [KvServer::GetSnapShotFromRaft](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L350)
- [KvServer::KvServer](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/kvServer.cpp#L377)

### 你要回答的问题

1. `KvServer` 到底维护了哪些状态？
2. `m_lastRequestId` 为什么能用于去重？
3. 为什么重复的 `Get` 可以再次执行，而重复的 `Put/Append` 不应再次生效？
4. `ApplyMsg` 在这个工程里扮演什么角色？
5. `KvServer` 为什么自己不关心日志复制细节，却能保证客户端看到的是一致结果？
6. 这份实现里快照是由哪一层决定生成、哪一层负责保存、哪一层负责安装？

### 建议你特别注意的点

- `KvServer` 构造函数里同时注册了 `kvServerRpc` 和 `raftRpc`
- 代码里通过 `sleep` 等待所有节点起来，这是实现层妥协，不是协议本身要求
- `waitApplyCh` 的 key 是 `raftIndex`，不是 `clientId`
- 真正的状态机操作发生在 `GetCommandFromRaft`

### 当天产出

写一页笔记，题目就叫“KvServer 不是状态机本身，而是协调层”。内容至少包括：

- `KvServer` 管哪些事
- `Raft` 管哪些事
- 两者通过什么数据结构交互

### 当天验收标准

你能回答“为什么超时后客户端重试不会导致 `Put` 被执行两次”。

---

## 第 3 天

### 目标

开始进入 `Raft` 核心，但先不试图一次吃完整个文件。第 3 天只盯“节点状态切换、选举超时、心跳触发”。

### 阅读顺序

1. [src/raftCore/include/raft.h](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/include/raft.h)
2. [src/raftCore/raft.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp)

### 精读函数

- [Raft::init](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp#L988)
- [Raft::electionTimeOutTicker](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp#L313)
- [Raft::doElection](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp#L202)
- [Raft::leaderHearBeatTicker](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp#L480)
- [Raft::doHeartBeat](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/raft.cpp#L247)

### 先记住的核心状态

- `m_currentTerm`
- `m_votedFor`
- `m_logs`
- `m_commitIndex`
- `m_lastApplied`
- `m_nextIndex`
- `m_matchIndex`
- `m_status`
- `m_lastResetElectionTime`
- `m_lastResetHearBeatTime`

### 你要回答的问题

1. 一个 follower 什么时候会变成 candidate？
2. candidate 在新一轮选举开始时要更新哪些状态？
3. leader 为什么需要周期性 heartbeat？
4. 这份代码里心跳和日志复制为什么共用 `AppendEntries`？
5. `nextIndex` 和 `matchIndex` 分别是站在 leader 视角记录什么？

### 当天产出

画一张状态图，包含：

- `Follower`
- `Candidate`
- `Leader`

并标明至少这些转换条件：

- 选举超时
- 收到更大 term 的 RPC
- 获得多数票
- 收到合法 leader 的心跳

### 当天验收标准

你能不看代码，自己说出一次“follower 超时发起选举”的执行步骤。

---

## 第 4 到第 14 天概览

### 第 4 天

继续读选举与投票相关逻辑，补 `RequestVote`、`sendRequestVote`、日志新旧判断。

### 第 5 到第 7 天

重点攻克日志复制：

- `AppendEntries1`
- `sendAppendEntries`
- `getPrevLogInfo`
- `leaderUpdateCommitIndex`
- `getApplyLogs`
- `Start`

目标是彻底理解：

- `prevLogIndex/prevLogTerm`
- 冲突回退
- `commitIndex`
- `lastApplied`
- why follower 不会无脑覆盖日志

### 第 8 天

只做一件事：把客户端请求从 `Start` 到 `apply` 再画一遍，这次要包含日志索引变化。

### 第 9 到第 10 天

读快照和持久化：

- [src/raftCore/Persister.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/raftCore/Persister.cpp)
- `Raft::Snapshot`
- `Raft::InstallSnapshot`
- `KvServer::IfNeedToSendSnapShotCommand`
- `KvServer::ReadSnapShotToInstall`

### 第 11 天

读 RPC 基座：

- [src/rpc/mprpcchannel.cpp](/home/wjp/learningc++/raft/KVstorageBaseRaft-cpp/src/rpc/mprpcchannel.cpp)
- `rpcprovider.cpp`

搞清楚 protobuf、header、socket 收发。

### 第 12 天

补 `skipList`、`common/util`、`raftRpcUtil`、`raftServerRpcUtil`，理解支撑件而不是陷进去抠细节。

### 第 13 天

从工程角度做一次审视：锁、线程、sleep、异常恢复、可能的竞态。

### 第 14 天

输出最终总结，至少完成：

1. 请求全链路图
2. Raft 状态切换图
3. 日志复制图
4. 快照流转图
5. 一份“这个实现和论文有哪些对应关系、有哪些工程化简化”的总结

---

## 每天固定动作

每天都做这四件事：

1. 读代码前先写下今天要回答的问题。
2. 读完后自己关闭编辑器，凭记忆复述主流程。
3. 记录 3 个“我已经确认的事实”和 2 个“我仍然不确定的点”。
4. 产出一张图或半页文字总结。

---

## 判断自己是否真的理解了

如果你已经能独立回答下面这些问题，说明两周目标基本达成：

1. 为什么客户端需要 `clientId + requestId`？
2. 为什么 leader 本地接到写请求后不能立刻返回成功？
3. follower 收到 `AppendEntries` 后要先检查什么？
4. `nextIndex` 回退解决了什么问题？
5. `commitIndex` 和 `lastApplied` 为什么必须分开？
6. 为什么状态机去重通常放在 `KvServer` 而不是 `Clerk`？
7. 快照为什么不能只在 Raft 层理解，必须和状态机层一起看？
8. 这份实现里最容易出 bug 的并发点在哪里？

---

## 建议的学习纪律

- 第 1 周不要急着改代码，先理解现有实现。
- 第 2 周可以开始做小实验，比如加日志、断点、模拟 leader 切换。
- 每次只抓一条主线，不要在一个晚上同时啃 `fiber`、`RPC`、`Raft` 三件事。
- 如果你解释一个函数时总要重复念代码，说明你还没建立抽象。

---

## 最后的标准

两周后你至少应该达到这三个层次：

1. 能讲清主流程
2. 能解释关键状态为什么存在
3. 能指出这份实现相对标准 Raft 的简化和工程风险
